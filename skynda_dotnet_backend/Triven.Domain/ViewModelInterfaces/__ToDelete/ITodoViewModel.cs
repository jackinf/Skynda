namespace Triven.Domain.ViewModelInterfaces
{
    /// <summary>
    /// Just a base interface to use for implementation for each view model.
    /// In domain we need to describe service that the returned type should be some kind of view model.
    /// We can also use "Find usages" and find all the implemented view models.
    /// TODO: Maybe we can be more specific and define properties?
    /// </summary>
    public interface ITodoViewModel
    {
        
    }
}